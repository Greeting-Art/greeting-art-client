/* eslint-disable indent */
import React from 'react';
import PropTypes from 'prop-types';

function ArtCanvas(props) {
    return (
        <div>
            
        </div>
    );
}

ArtCanvas.propTypes = {

};

export default ArtCanvas;
